<% songs.each do |song| %> 
    <div class = "song-card">
    <p> <%= song.title %> </p>
    <p> <%= song.artist.name %> </p>
    <date> <%= song.created_at.to_s(:short) %> </date>
    </div>
<% end %>